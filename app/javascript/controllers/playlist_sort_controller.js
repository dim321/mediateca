import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  static targets = ["list"]

  connect() {
    // Placeholder for Sortable.js integration
    // Will be implemented with drag-and-drop reordering
  }
}
